import 'package:flutter/material.dart';

class ChatScreenTablet extends StatelessWidget {
  const ChatScreenTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
